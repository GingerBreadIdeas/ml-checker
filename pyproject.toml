[project]
name = "ml-checker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []


[tool.black]
line-length = 79
[tool.isort]
profile = "black"

[tool.uv.workspace]
members = [
    "runner",
    "scripts",
]
